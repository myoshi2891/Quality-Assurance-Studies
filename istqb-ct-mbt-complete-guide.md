# 🧩 モデルベーステスト（MBT）完全ガイド

## ISTQB CT-MBT (Certified Tester Model-Based Tester) 準拠

> **対象読者:** MBT 初学者〜中級者  
> **参考規格:** ISTQB CT-MBT v1.1 シラバス | 最新学術論文 | 業界ベストプラクティス（2025〜2026年）  
> **最終更新:** 2026年4月

---

## 📚 目次

1. [MBTとは何か？—概念と定義](#1-mbtとは何か概念と定義)
2. [なぜMBTが必要か？—動機とビジネス価値](#2-なぜmbtが必要か動機とビジネス価値)
3. [MBTの全体プロセスと成果物](#3-mbtの全体プロセスと成果物)
4. [SDLCへのMBT統合](#4-sdlcへのmbt統合)
5. [MBTモデリングの基礎](#5-mbtモデリングの基礎)
6. [MBTモデルの言語・記法](#6-mbtモデルの言語記法)
7. [モデリングのグッドプラクティス](#7-モデリングのグッドプラクティス)
8. [テストケース生成の選択基準（Selection Criteria）](#8-テストケース生成の選択基準selection-criteria)
9. [MBTテストの実装と実行](#9-mbtテストの実装と実行)
10. [テスト適応（Test Adaptation）活動](#10-テスト適応test-adaptation活動)
11. [MBTアプローチの評価と展開](#11-mbtアプローチの評価と展開)
12. [MBTと従来テスト設計技法の関係](#12-mbtと従来テスト設計技法の関係)
13. [MBTツール詳細比較](#13-mbtツール詳細比較)
14. [GraphWalker実践チュートリアル](#14-graphwalker実践チュートリアル)
15. [MBTの課題と解決策](#15-mbtの課題と解決策)
16. [AI・LLMとMBTの融合（2025年最新動向）](#16-aillmとmbtの融合2025年最新動向)
17. [ISTQB CT-MBT 試験対策](#17-istqb-ct-mbt-試験対策)
18. [参考URL一覧](#18-参考url一覧)

---

## 1. MBTとは何か？—概念と定義

### 1.1 定義（ISTQB公式）

> **「モデルベーステスト（MBT）とは、モデルに基づく、またはモデルを含む、あらゆる種類のテストである。」**  
> — ISTQB CT-MBT Syllabus v1.1

より具体的には：  
**テスト対象システム（SUT: System Under Test）とその環境の形式的モデルを使って、テストケースを体系的に生成・実行・評価するブラックボックステスト技法**です。

### 1.2 MBTの位置づけ（従来テストとの違い）

> ◆ 従来のテストアプローチ
> ┌──────────────────────────────────────────────────────────────┐
> │  要件定義書 ──→ テスターが手動でテストケースを設計 ──→ 実行   │
> │                                                              │
> │  問題点:                                                      │
> │  ・設計者の経験・スキルに大きく依存                             │
> │  ・再現性・一貫性の欠如                                        │
> │  ・要件変更時の保守コストが高い                                 │
> │  ・エッジケースの見落とし                                       │
> └──────────────────────────────────────────────────────────────┘

> ◆ MBT（モデルベーステスト）アプローチ
> ┌──────────────────────────────────────────────────────────────┐
> │  要件定義書 ──→ モデルを作成 ──→ ツールが自動でテストケース生成│
> │                    (FSM/UML/         ──→ 実行 ──→ 評価       │
> │                  状態遷移図 等)                               │
> │                                                              │
> │  メリット:                                                    │
> │  ・自動・体系的なテストケース生成                               │
> │  ・高いカバレッジ・再現性                                       │
> │  ・モデル更新→テスト自動再生成                                  │
> │  ・コミュニケーションの共通言語                                 │
> └──────────────────────────────────────────────────────────────┘

### 1.3 MBTの歴史的背景

| 時代 | 出来事 |
|------|--------|
| **1970年代** | T.S. Chow（Bell研究所）が有限状態機械（FSM）を使ったテスト生成を提唱。MBTの起源 |
| **1990年代〜** | 学術・産業界でMBTへの関心が急増。UML・BPMNとの統合が進む |
| **2000年代〜** | 産業界での実用化。MBTツールの商用化が本格化 |
| **2012年** | Utting・Pretschner・Legeardによる「MBT分類法」論文（1,000回以上引用）が体系化を推進 |
| **2019年** | ISTQB CT-MBT シラバス v1.0 リリース。v1.1に更新 |
| **2021〜2025年** | LLM・AIとMBTの融合。自動モデル推論技術の発展。Grokipedia調査ではLLMによるモデル自動生成の事例が報告 |

---

## 2. なぜMBTが必要か？—動機とビジネス価値

### 2.1 MBT導入の定量的効果（実績データ）

> ┌─────────────────────────────────────────────────────────────────┐
> │                   MBT導入による実績効果                          │
> ├─────────────────────────────────┬────────────────────────────────┤
> │  指標                           │  効果（実証データ）             │
> ├─────────────────────────────────┼────────────────────────────────┤
> │  テストスイート開発時間          │  50%削減（2人月 → 4人月の比較） │
> │  テストカバレッジ               │  38,460ステップ vs 2,658ステップ │
> │  プロジェクトコスト削減         │  最大64%削減の可能性             │
> │  Microsoft での採用効果         │  大規模テスト自動化の実現        │
> ├─────────────────────────────────┴────────────────────────────────┤
> │  出典: Grokipedia - Model-based testing (2026)                   │
> │  https://grokipedia.com/page/Model-based_testing                 │
> └─────────────────────────────────────────────────────────────────┘

### 2.2 MBTの主要メリット7つ

> 1. 🔍 早期欠陥検出（Early Defect Detection）
>    └─ モデル構築時点で要件の矛盾・不整合が発覚
>    └─ 実装フェーズ前にバグを発見 → 修正コストを劇的に削減

> 2. 📊 高いテストカバレッジ（Comprehensive Coverage）
>    └─ 状態・遷移・パス等を体系的に網羅
>    └─ 手動テストが見落としがちなエッジケースも自動検出

> 3. 🔄 高い再現性（Reproducibility）
>    └─ モデルから常に一貫したテストケースを生成
>    └─ 回帰テストの信頼性が向上

> 4. 🔧 保守コストの削減（Reduced Maintenance）
>    └─ 要件変更時はモデル更新→テストケース自動再生成
>    └─ 静的なテストスクリプト管理が不要に

> 5. 🤝 コミュニケーション改善（Better Communication）
>    └─ モデルが開発者・テスター・ビジネス側の共通言語に
>    └─ 視覚的なモデルで要件の理解が深まる

> 6. 🚀 テスト効率化（Automation-Friendly）
>    └─ テスト生成の自動化でチームは分析・改善に集中できる

> 7. 🔁 再利用性（Reusability）
>    └─ 作成したモデルを回帰テスト・類似プロジェクトで再利用

### 2.3 MBTが特に効果的なシステム

| システムタイプ | 理由 |
|-------------|------|
| 組み込み・リアクティブシステム | 状態遷移が複雑、全状態の網羅が必須 |
| 通信プロトコル | 状態機械で自然に表現可能 |
| 車載システム（AUTOSAR等） | 並行処理・階層状態が多く手動テストが困難 |
| 金融・保険業務ロジック | 複雑な条件分岐、デシジョンテーブルが適合 |
| Web/モバイルアプリのUIフロー | ページ遷移・画面状態の体系的テスト |
| Agile/DevOps環境 | 頻繁な変更への素早い対応が必要な環境 |

---

## 3. MBTの全体プロセスと成果物

### 3.1 MBTプロセスの全体像

> ┌─────────────────────────────────────────────────────────────────────┐
> │                       MBTの全体フロー                                │
> │                                                                     │
> │  [Input]                                                            │
> │  要件・仕様・ユーザーストーリー                                         │
> │  設計文書・業務プロセス                                                │
> │        ↓                                                            │
> │  ┌─────────────────────────────────────────────────────────────┐   │
> │  │  Step 1: モデリング（Modeling）                               │   │
> │  │  ・モデル言語の選択（UML/FSM/BPMN等）                         │   │
> │  │  ・MBTモデルの作成（振る舞い・状態・遷移を記述）                │   │
> │  │  [成果物: MBT Model（状態遷移図 等）]                         │   │
> │  └─────────────────────────────────────────────────────────────┘   │
> │        ↓                                                            │
> │  ┌─────────────────────────────────────────────────────────────┐   │
> │  │  Step 2: テストケース生成（Test Case Generation）             │   │
> │  │  ・テスト選択基準の定義（カバレッジ基準等）                    │   │
> │  │  ・ツールによる自動テストケース生成                            │   │
> │  │  [成果物: 抽象テストケース（Abstract Test Cases）]            │   │
> │  └─────────────────────────────────────────────────────────────┘   │
> │        ↓                                                            │
> │  ┌─────────────────────────────────────────────────────────────┐   │
> │  │  Step 3: テスト適応（Test Adaptation）                       │   │
> │  │  ・抽象テストケース → 具体的・実行可能なテストスクリプトへ変換  │   │
> │  │  ・テストアダプターの実装（ブリッジコード）                    │   │
> │  │  [成果物: 具体テストケース（Concrete Test Cases）]            │   │
> │  └─────────────────────────────────────────────────────────────┘   │
> │        ↓                                                            │
> │  ┌─────────────────────────────────────────────────────────────┐   │
> │  │  Step 4: テスト実行（Test Execution）                        │   │
> │  │  ・SUT（テスト対象）に対してテストを実行                       │   │
> │  │  ・結果の記録・ログ収集                                        │   │
> │  │  [成果物: テスト結果レポート]                                  │   │
> │  └─────────────────────────────────────────────────────────────┘   │
> │        ↓                                                            │
> │  ┌─────────────────────────────────────────────────────────────┐   │
> │  │  Step 5: 評価（Evaluation）                                  │   │
> │  │  ・テスト結果の評価                                            │   │
> │  │  ・モデル・アプローチの振り返り                                │   │
> │  └─────────────────────────────────────────────────────────────┘   │
> │                                                                     │
> │  ※各Stepは反復的・並行的に実施されることが多い                          │
> └─────────────────────────────────────────────────────────────────────┘

### 3.2 MBTの主要成果物（Artifacts）一覧

| 成果物名 | 説明 | 例 |
|---------|------|-----|
| **MBTモデル** | システムの期待振る舞いを表現 | 状態遷移図、UMLダイアグラム |
| **テスト選択基準** | どこまでテストするかの基準 | 遷移カバレッジ、パスカバレッジ |
| **抽象テストケース** | モデルから生成された高レベルなテスト | 「状態AからBへの遷移を実行」 |
| **具体テストケース** | 実際のSUTで実行可能な形式 | Seleniumスクリプト、Appium等 |
| **テストアダプター** | 抽象と具体を橋渡しするコード | Java/Python アダプタークラス |
| **テストオラクル** | 期待結果の定義 | モデルから自動導出 or 手動定義 |
| **トレーサビリティマトリクス** | 要件⇔モデル⇔テストの対応表 | 要件カバレッジ管理 |

---

## 4. SDLCへのMBT統合

### 4.1 ウォーターフォール型への統合

> 要件定義フェーズ
> ├── MBTモデルを要件から作成開始
> ├── モデルにより要件の不整合を早期発見
> └── [成果物] 初期MBTモデル

> 設計フェーズ
> ├── 設計情報をモデルに反映・精緻化
> └── [成果物] 精緻化MBTモデル

> 開発フェーズ
> ├── モデルからテストケースを自動生成
> └── [成果物] 抽象テストケース

> テストフェーズ
> ├── 抽象テストケース → 具体テストスクリプトへ変換
> ├── テスト実行・結果収集
> └── [成果物] テスト結果・不具合レポート

> リリース後
> └── モデルを回帰テストに再利用

### 4.2 アジャイル開発への統合（CT-MBT重要項目）

> Sprint 0（初回スプリント前）
> └── MBTツール・モデリング言語を選定

> 各Sprintでの流れ
> ┌──────────────────────────────────────────────────────────┐
> │  Sprint N                                                │
> │                                                          │
> │  1. Sprint計画                                           │
> │     └─ ユーザーストーリー → MBTモデルへの反映計画           │
> │  2. 開発                                                 │
> │     └─ 開発者がMBTモデルを参照しながらコーディング           │
> │  3. MBTモデル更新                                        │
> │     └─ 新機能・変更をモデルに反映                           │
> │  4. テスト生成・実行                                      │
> │     └─ 更新モデルから新テストケースを自動生成               │
> │  5. スプリントレビュー                                    │
> │     └─ MBTカバレッジレポートを共有                         │
> └──────────────────────────────────────────────────────────┘

> ✅ **アジャイルでのMBTポイント:**  
> - 軽量なモデリングアプローチを選択する（ヘビーウェイトな形式手法は避ける）  
> - CI/CDパイプラインにMBTツールを組み込む  
> - モデルは「生きた文書」として継続的に更新する

### 4.3 DevOpsパイプラインへの統合

> コードコミット → MBTモデル更新 → テストケース自動生成
> ↓
> CI/CD パイプライン
> ├── MBTモデルの検証（model validation）
> ├── テストケース自動生成
> ├── 生成テストの実行（実機 or クラウド環境）
> ├── カバレッジレポート生成
> └── 品質ゲート判定 → 合格: マージ / 不合格: 差し戻し

---

## 5. MBTモデリングの基礎

### 5.1 モデルとは何か

**MBTにおけるモデル**とは、「テストの視点から見たシステムの期待振る舞いの抽象化表現」です。

> MBTモデルが表現するもの
> ├── 機能モデル（Functional Model）
> │   └─ 機能・入出力の関係を表現
> │   └─ 例: デシジョンテーブル、真理値表
> │
> ├── 振る舞いモデル（Behavioral Model）
> │   └─ システムの状態・遷移・イベントを表現
> │   └─ 例: 有限状態機械（FSM）、状態遷移図
> │
> └── 構造モデル（Structural Model）
> └─ システムのコンポーネント間の関係を表現
> └─ 例: UMLクラス図、コンポーネント図

### 5.2 有限状態機械（FSM）の基礎

**FSM（Finite State Machine）**はMBTで最もよく使われるモデルです。

**構成要素:**
- **状態（State）:** システムが取りうる各状態（例：ログイン前、ログイン中、エラー）
- **遷移（Transition）:** ある状態から別の状態への移動
- **イベント（Event）:** 遷移を引き起こすトリガー（例：ボタンクリック）
- **アクション（Action）:** 遷移時に実行される処理
- **ガード（Guard）:** 遷移条件（例：[パスワードが正しい場合]）

**具体例：ログイン機能のFSM**

> 状態: S0=初期画面, S1=ログイン成功, S2=エラー表示, S3=アカウントロック

>          ログイン試行[PW正]
> S0 ─────────────────────────────→ S1（ログイン成功）
> │                                       │
> │ ログイン試行[PW誤/1〜2回目]           │ ログアウト
> ↓                                       │
> S2（エラー表示）                          ↓
> │                                      S0
> │ 再試行[PW誤/3回目]
> ↓
> S3（アカウントロック）

> 状態遷移表:
> ┌────────┬─────────────────────────┬─────────────────────────┬───────────────┐
> │ 現在状態│ イベント                  │ ガード条件               │ 次の状態       │
> ├────────┼─────────────────────────┼─────────────────────────┼───────────────┤
> │  S0    │ ログインボタンクリック      │ パスワード正              │ S1            │
> │  S0    │ ログインボタンクリック      │ パスワード誤（1〜2回）     │ S2            │
> │  S0    │ ログインボタンクリック      │ パスワード誤（3回目）      │ S3            │
> │  S1    │ ログアウト                │ −                       │ S0            │
> │  S2    │ 再入力                   │ −                       │ S0            │
> │  S3    │ 管理者リセット             │ −                       │ S0            │
> └────────┴─────────────────────────┴─────────────────────────┴───────────────┘

### 5.3 状態遷移テストの種類

> カバレッジレベル（低→高）
> ├── 0スイッチカバレッジ（状態カバレッジ）
> │   └─ すべての状態を少なくとも1回訪問
> │
> ├── 1スイッチカバレッジ（遷移カバレッジ）
> │   └─ すべての遷移を少なくとも1回実行
> │
> ├── 2スイッチカバレッジ
> │   └─ すべての長さ2の遷移シーケンスを実行
> │
> ├── Nスイッチカバレッジ
> │   └─ すべての長さNの遷移シーケンスを実行
> │
> └── 全パスカバレッジ（All-Path Coverage）
> └─ 可能な全経路を実行（通常は実行不可能なほど膨大）

---

## 6. MBTモデルの言語・記法

### 6.1 主要モデリング言語の全体マップ

> MBTで使うモデリング言語・記法
> │
> ├── 状態ベース（State-Based）
> │   ├── 有限状態機械（FSM）—最も基本的、シンプルな振る舞い
> │   ├── 状態チャート（Statecharts）—FSMの拡張版、階層化・並行処理対応
> │   └── ラベル付き遷移システム（LTS）—形式的なアプローチ
> │
> ├── フロー/アクティビティベース
> │   ├── UML アクティビティ図—ワークフロー、処理の流れ
> │   ├── BPMN（Business Process Model and Notation）—業務プロセス
> │   └── フローチャート—制御フローの表現
> │
> ├── データ/ロジックベース
> │   ├── デシジョンテーブル（Decision Tables）—複雑な条件分岐
> │   ├── UML クラス図—データモデル、状態変数の定義
> │   └── データフロー図（DFD）—データの流れと変換
> │
> ├── シナリオ/ユースケースベース
> │   ├── UML ユースケース図—機能要件のモデル
> │   ├── UML シーケンス図—オブジェクト間のメッセージ交換
> │   └── ユーザーストーリー（Agile）
> │
> └── 確率的モデル
> └── マルコフ連鎖（Markov Chain）—使用頻度に基づく統計的テスト

### 6.2 各モデル言語の詳細比較

> ┌──────────────────┬────────────────────────┬─────────────────┬─────────────────┐
> │  記法             │  適用シナリオ            │  ツール例        │  難易度         │
> ├──────────────────┼────────────────────────┼─────────────────┼─────────────────┤
> │ 有限状態機械(FSM) │ 組み込み、プロトコル      │ GraphWalker,    │ ★★☆☆☆        │
> │                  │ ログインフロー            │ Conformiq       │                │
> ├──────────────────┼────────────────────────┼─────────────────┼─────────────────┤
> │ 状態チャート      │ 車載システム、複雑UI      │ MATLAB/Simulink │ ★★★☆☆        │
> │ (Statecharts)    │ 階層・並行処理あり        │ Yakindu         │                │
> ├──────────────────┼────────────────────────┼─────────────────┼─────────────────┤
> │ UMLアクティビティ│ ワークフロー、業務プロセス │ MBTsuite,       │ ★★☆☆☆        │
> │ 図               │                         │ IBM RTW         │                │
> ├──────────────────┼────────────────────────┼─────────────────┼─────────────────┤
> │ デシジョンテーブル│ 金融ロジック、            │ TOSCA,          │ ★★★☆☆        │
> │                  │ 保険料計算               │ TestComplete    │                │
> ├──────────────────┼────────────────────────┼─────────────────┼─────────────────┤
> │ BPMN             │ 企業の業務プロセス        │ BPM-X           │ ★★★★☆        │
> ├──────────────────┼────────────────────────┼─────────────────┼─────────────────┤
> │ マルコフ連鎖      │ 統計的使用テスト          │ SMC             │ ★★★★★        │
> │                  │ 信頼性テスト             │                 │                │
> └──────────────────┴────────────────────────┴─────────────────┴─────────────────┘

### 6.3 UML ステートマシン図の記法例

> UML ステートマシン記法の主要要素:

> 【初期状態】: ●（黒塗り円）
> 【状態】    : 角丸矩形、内部に状態名
> 【終了状態】: ◎（二重円）
> 【遷移】    : 矢印（→）+ ラベル: イベント[ガード]/アクション

> 例：ATMの操作フロー

>         ●
>         ↓
> ┌──────────────┐   カード挿入    ┌──────────────┐
> │    待機中     │ ──────────────→│  PIN入力待ち  │
> └──────────────┘                └──────────────┘
> ↑                              │
> │                    PIN正 │  │ PIN誤×3
> │                          ↓  ↓
> │               ┌──────────────┐   ┌──────────────┐
> │               │  操作選択中   │   │  カード没収   │──→ ◎
> │               └──────────────┘   └──────────────┘
> │                      │
> │ カード排出            │ 取引完了
> └──────────────────────┘

### 6.4 デシジョンテーブルの記法例

> 例：オンラインショッピングの割引ロジック

> 条件（Conditions）:
> ┌──────────────────────┬────┬────┬────┬────┬────┐
> │                       │ R1 │ R2 │ R3 │ R4 │ R5 │
> ├──────────────────────┼────┼────┼────┼────┼────┤
> │ 会員ランク = ゴールド │ Y  │ Y  │ N  │ N  │ N  │
> │ 購入金額 ≥ 10,000円  │ Y  │ N  │ Y  │ N  │ N  │
> │ クーポン保有          │ Y  │ Y  │ N  │ Y  │ N  │
> ├──────────────────────┼────┼────┼────┼────┼────┤
> アクション（Actions）:
> │ 割引率 30%            │ X  │    │    │    │    │
> │ 割引率 20%            │    │ X  │ X  │    │    │
> │ 割引率 10%            │    │    │    │ X  │    │
> │ 割引なし              │    │    │    │    │ X  │
> └──────────────────────┴────┴────┴────┴────┴────┘

> R1〜R5 の各列がテストケースになる

---

## 7. モデリングのグッドプラクティス

### 7.1 MBTモデルの品質特性

> 高品質なMBTモデルが持つべき特性
> ├── ✅ 正確性（Correctness）
> │   └─ モデルがシステムの正しい振る舞いを表現している
> │
> ├── ✅ 完全性（Completeness）
> │   └─ テスト目的に関係するすべての振る舞いが網羅されている
> │
> ├── ✅ 一貫性（Consistency）
> │   └─ モデル内に矛盾がない
> │
> ├── ✅ 理解可能性（Understandability）
> │   └─ チームメンバーが読める・理解できる
> │
> ├── ✅ 保守性（Maintainability）
> │   └─ 要件変更時に容易に更新できる
> │
> └── ✅ テスト適合性（Testability）
> └─ モデルからテストケースを生成できる構造

### 7.2 よくある間違いとアンチパターン

> ❌ アンチパターン1: モデルが過剰に詳細
> 問題: 実装の詳細までモデル化してしまい、変更のたびに大幅修正が必要
> 対策: テスト目的に必要な抽象度を維持する

> ❌ アンチパターン2: 状態爆発（State Explosion）
> 問題: 変数の組み合わせで状態数が爆発的に増加
> 対策: 状態チャートの階層化・直交コンポーネント分割

> ❌ アンチパターン3: デッドステート（到達不能な状態）
> 問題: どの遷移からも到達できない状態が存在
> 対策: モデル検証ツールで到達可能性を確認

> ❌ アンチパターン4: テストオラクルの欠如
> 問題: モデルが振る舞いを定義しているが、期待結果が定義されていない
> 対策: 事前/事後条件・ガードにアサーションを追加

> ❌ アンチパターン5: 要件とモデルの乖離
> 問題: モデルが要件から独立して"勝手に進化"し同期が取れなくなる
> 対策: トレーサビリティ管理ツールで要件⇔モデルを紐付け

### 7.3 反復的モデル開発のアプローチ

> Phase 1: 初期モデル（スコープ定義）
> └─ 主要な状態と主フローのみ記述（Happy Pathから始める）

> Phase 2: 拡張（エラー処理・代替フロー追加）
> └─ 異常系・例外ケースを追加

> Phase 3: 詳細化（ガード条件・データ追加）
> └─ 条件分岐・入力データ範囲を具体化

> Phase 4: 検証（モデル検証・レビュー）
> └─ ツールによる形式的検証 + 人によるレビュー

> Phase 5: テスト生成・評価
> └─ テストケース生成 → 実行 → モデルへフィードバック

### 7.4 要件とモデルのトレーサビリティ

> 要件ID: REQ-001 「ユーザーはID/PWでログインできること」
> ↓ トレース
> MBTモデル要素: 状態「S0→S1の遷移」「ガード: PW正」
> ↓ トレース  
> テストケース: TC-001「正常ログインテスト」
> ↓ トレース
> テスト結果: PASS / FAIL

> → トレーサビリティマトリクスで要件カバレッジを可視化

---

## 8. テストケース生成の選択基準（Selection Criteria）

### 8.1 選択基準の6ファミリー（CT-MBT シラバス準拠）

CT-MBT シラバスでは、最も広く使われる6つのテスト選択基準ファミリーを定義しています。

> ┌─────────────────────────────────────────────────────────────────┐
> │         MBT テスト選択基準の6ファミリー                           │
> ├──────────────────────┬──────────────────────────────────────────┤
> │  ファミリー            │  説明・例                               │
> ├──────────────────────┼──────────────────────────────────────────┤
> │ 1. 構造ベース         │ モデルの構造要素を網羅                     │
> │    (Structural-based) │ 例: 状態カバレッジ、遷移カバレッジ          │
> ├──────────────────────┼──────────────────────────────────────────┤
> │ 2. データベース        │ 入力データの組み合わせを網羅               │
> │    (Data-based)       │ 例: 同値分割、境界値分析                  │
> ├──────────────────────┼──────────────────────────────────────────┤
> │ 3. 要件ベース         │ 要件ごとのカバレッジを確保                 │
> │    (Requirements-based)│ 例: 要件トレーサビリティカバレッジ         │
> ├──────────────────────┼──────────────────────────────────────────┤
> │ 4. ランダム           │ モデルをランダムに探索                     │
> │    (Random)           │ 例: ランダムウォーク                      │
> ├──────────────────────┼──────────────────────────────────────────┤
> │ 5. フォールトベース    │ 想定される欠陥の検出を優先                 │
> │    (Fault-based)      │ 例: ミューテーションテスト基準              │
> ├──────────────────────┼──────────────────────────────────────────┤
> │ 6. シナリオ/ユースケース│ 実際の使用シナリオを優先                  │
> │    ベース             │ 例: ユーザーストーリーカバレッジ            │
> └──────────────────────┴──────────────────────────────────────────┘

### 8.2 構造ベース基準の詳細（最重要）

#### 状態カバレッジ（State Coverage）

> 定義: モデル内の全状態を少なくとも1回訪問すること

> 例（ログインFSM）:
> 状態: S0（初期）, S1（成功）, S2（エラー）, S3（ロック）

> テストケース:
> TC-1: S0 → S1（正常ログイン）         ✅ S0, S1 カバー
> TC-2: S0 → S2 → S0（1回失敗）        ✅ S2 カバー
> TC-3: S0 → S2 → S2 → S3（3回失敗）  ✅ S3 カバー

> 状態カバレッジ: 4/4 = 100%

#### 遷移カバレッジ（Transition Coverage）

> 定義: モデル内の全遷移を少なくとも1回実行すること

> FSMの全遷移:
> T1: S0 → S1 [PW正]
> T2: S0 → S2 [PW誤1〜2回目]
> T3: S0 → S3 [PW誤3回目]
> T4: S1 → S0 [ログアウト]
> T5: S2 → S0 [再入力]
> T6: S3 → S0 [管理者リセット]

> 各遷移を少なくとも1回通るテストセットが必要
> 遷移カバレッジ = (実行した遷移数 / 総遷移数) × 100%

#### パスカバレッジ（Path Coverage）

> 定義: モデル内の全パスを実行すること

> 種類:
> ├── 最短パスカバレッジ
> │   └─ 開始から終了への最短経路のみ
> ├── 全パスカバレッジ（理論上）
> │   └─ 循環があると無限になりうるため通常は非現実的
> └── N回ループカバレッジ
> └─ ループを最大N回繰り返すパスを生成

### 8.3 データベース基準の詳細

> 同値分割（Equivalence Partitioning）
> ↓ MBTでの応用

> モデルの各遷移のガード条件に対してデータパーティションを定義:

> 例: 年齢確認ロジック（最低年齢18歳）
> ┌──────────────────────────────────────────────────────────────┐
> │ 入力値の同値クラス:                                           │
> │   クラス1（有効）: 18以上  → テスト値例: 18, 25, 65         │
> │   クラス2（無効）: 18未満  → テスト値例: 0, 17, -1          │
> │                                                              │
> │ 境界値分析:                                                  │
> │   境界値: 17（無効上限）、18（有効下限）、19（有効内）        │
> │   → 最低限 3ケースが必要                                     │
> └──────────────────────────────────────────────────────────────┘

### 8.4 選択基準の選び方ガイド

> Q1: プロジェクトのリスクレベルは？
> ├── HIGH（安全クリティカル/航空・医療等）
> │   → 高カバレッジ基準（Nスイッチ + フォールトベース）
> └── MEDIUM/LOW
> → 遷移カバレッジ + データベース基準

> Q2: テスト時間・コストの制約は？
> ├── 制約が厳しい → ランダム or シナリオベース
> └── 制約が緩い   → 構造ベース + フォールトベース

> Q3: システムのタイプは？
> ├── 状態が複雑（組み込み/通信）→ 状態/遷移カバレッジ
> ├── データが複雑（金融/保険）   → データベース基準
> └── ビジネスロジック中心        → 要件/シナリオベース

---

## 9. MBTテストの実装と実行

### 9.1 オフラインMBT vs オンラインMBT

> ◆ オフラインMBT（Offline MBT）
> ┌────────────────────────────────────────────────────────────┐
> │  1. MBTツールでテストケースを「事前に」一括生成              │
> │  2. 生成されたテストケースをファイルに保存                   │
> │  3. 別途テスト実行フレームワーク（Selenium等）で実行         │
> │                                                            │
> │  メリット: 既存テストフレームワークと統合しやすい             │
> │  デメリット: モデルとSUTの同期が静的                        │
> └────────────────────────────────────────────────────────────┘

> ◆ オンラインMBT（Online MBT）
> ┌────────────────────────────────────────────────────────────┐
> │  1. MBTツールがSUTの現在の状態をリアルタイムに観察          │
> │  2. 観察結果に応じて次のテストアクションを動的に決定         │
> │  3. テスト生成と実行が同時進行（adaptive testing）          │
> │                                                            │
> │  メリット: SUTの実際の動作に適応できる、ランダム探索が有効   │
> │  デメリット: ツールとSUTのリアルタイム連携が必要             │
> └────────────────────────────────────────────────────────────┘

### 9.2 テスト実装の流れ（具体例）

> Step 1: 抽象テストケースの確認（ツール出力）
> 例: GraphWalkerが生成するパス
> → v_初期画面 → e_ログインボタン → v_ログイン成功 → e_ログアウト → v_初期画面

> Step 2: テストアダプター（Test Adapter）の実装
> ┌─────────────────────────────────────────────────────────────┐
> │ Java（GraphWalker + Selenium）のアダプター実装例             │
> │                                                             │
> │ public class LoginPageAdapter implements LoginModel {       │
> │                                                             │
> │   WebDriver driver;                                         │
> │                                                             │
> │   // 状態（Vertex）: ログイン画面の検証                      │
> │   @Override                                                 │
> │   public void v_初期画面() {                                │
> │     // アサーション：ログイン画面が表示されているか確認        │
> │     assertTrue(driver.findElement(By.id("loginForm"))       │
> │                .isDisplayed());                             │
> │   }                                                         │
> │                                                             │
> │   // エッジ（Edge）: ログインボタンのクリックアクション       │
> │   @Override                                                 │
> │   public void e_ログインボタン() {                          │
> │     // アクション：ID・PWを入力してログインボタンをクリック   │
> │     driver.findElement(By.id("username"))                   │
> │           .sendKeys("testuser");                            │
> │     driver.findElement(By.id("password"))                   │
> │           .sendKeys("password123");                         │
> │     driver.findElement(By.id("loginBtn")).click();          │
> │   }                                                         │
> │ }                                                           │
> └─────────────────────────────────────────────────────────────┘

> Step 3: テスト実行
> → GraphWalkerがモデルを解析し生成したパスに沿って
> 上記アダプターを順に呼び出す

### 9.3 テストオラクルの定義

> テストオラクル（Test Oracle）: 「テスト結果が正しいか判断する仕組み」

> MBTでのオラクル定義方法:
> ├── モデルベースオラクル
> │   └─ モデルの事後条件（post-conditions）から自動導出
> │   └─ 例: 「S1に遷移したならWelcomeメッセージが表示される」
> │
> ├── 仕様ベースオラクル
> │   └─ 要件定義書・仕様書から定義
> │
> └── 比較ベースオラクル
> └─ 旧バージョンとの出力比較
> └─ 例: 前バージョンのAPIレスポンスを正解として使用

---

## 10. テスト適応（Test Adaptation）活動

### 10.1 テスト適応とは

**抽象テストケース**（ツールが生成した、SUTに依存しない高レベルなテスト）を  
**具体テストケース**（実際のSUTで実行できる具体的なコード/スクリプト）に変換するプロセスです。

> [抽象テストケース]               [具体テストケース]
> 「ユーザーがログインを実行する」 → driver.findElement(By.id("loginBtn")).click()
> 「システムが成功応答を返す」     → assertEquals(200, response.getStatusCode())
> 「ダッシュボードが表示される」   → assertTrue(dashboard.isDisplayed())

### 10.2 テストアダプターの3種類

> 1. 手動実装アダプター
>    └─ テスターがコードで直接実装
>    └─ 最大の柔軟性・最もコスト高

> 2. コード生成アダプター
>    └─ MBTツールが基本的なアダプタースケルトンを自動生成
>    └─ テスターが詳細を埋める（semi-automatic）

> 3. キーワード駆動アダプター
>    └─ キーワード辞書を使って抽象アクションをマッピング
>    └─ 例: 「Click(id=loginBtn)」というキーワードでアダプター不要

### 10.3 モデル・テストの変更管理

> 変更が発生した際のMBT対応フロー:

> 要件変更発生
> ↓
> 1. MBTモデルを更新（影響範囲を特定し修正）
>     ↓
> 2. MBTツールでテストケースを再生成
>     ↓
> 3. テストアダプターの修正（必要な場合のみ）
>     ↓
> 4. 回帰テストを実行（変更の影響範囲を検証）
>     ↓
> 5. カバレッジレポートで影響確認

> → 従来の手動テスト管理と比べ保守コストを大幅削減

---

## 11. MBTアプローチの評価と展開

### 11.1 MBT導入前の評価チェックリスト

> 組織・プロジェクトの適合性評価:

> □ システムの複雑性
> - 状態遷移・条件分岐が多いシステムか？ （YES → MBT有効）
> - 手動テスト設計が困難なほど複雑か？    （YES → MBT強く推奨）

> □ チームのスキル
> - チームにモデリング経験者はいるか？
> - 適切なトレーニングへの投資は可能か？

> □ ツールの準備
> - SUTにMBTツールが対応しているか？
> - 既存CI/CDとの統合は可能か？

> □ 要件の安定性
> - 要件が十分に明確か？（モデル作成可能なレベル）
> - 頻繁な変更があっても対応できるか？

> □ ROI（投資対効果）
> - テストコストの削減が見込めるか？
> - 長期プロジェクトでの再利用が可能か？

### 11.2 MBT展開の成熟度モデル

> レベル1: 試験的導入（Pilot）
> ├─ 単一機能・単一チームでの試験運用
> ├─ 簡単なFSMモデルから開始
> └─ 効果測定・教訓収集

> レベル2: 部分的展開（Partial Deployment）
> ├─ 複数機能・チームへの展開
> ├─ モデリングガイドラインの整備
> └─ CI/CDへの統合開始

> レベル3: 組織的展開（Organizational Deployment）
> ├─ 全プロジェクトへの標準化
> ├─ モデル・テストケースのライブラリ整備
> └─ 継続的な効果測定と改善

> レベル4: 最適化（Optimized）
> ├─ AIを活用した自動モデル生成
> ├─ リスクベース選択基準の高度化
> └─ 組織全体でのナレッジ共有

### 11.3 KPIと効果測定

> MBT効果測定のKPI例:
> ┌─────────────────────────────┬────────────────────────────────────┐
> │  KPI                        │  測定方法                          │
> ├─────────────────────────────┼────────────────────────────────────┤
> │ テストカバレッジ（%）        │ 遷移/状態カバレッジ率               │
> │ テスト設計時間              │ MBT導入前後の比較                   │
> │ 欠陥検出率                  │ テストで発見した欠陥数/総欠陥数      │
> │ 回帰テスト実行時間          │ モデル更新→テスト再実行までの時間    │
> │ テスト保守コスト            │ 要件変更あたりのテスト修正工数       │
> │ 要件トレーサビリティ        │ 要件→テストケースの紐付け率         │
> └─────────────────────────────┴────────────────────────────────────┘

---

## 12. MBTと従来テスト設計技法の関係

### 12.1 MBTは従来技法を「拡張」する

CT-MBTシラバスが強調する重要概念：  
> **「MBTは従来のテスト設計技法を置き換えるのではなく、拡張・強化するものである」**

> 従来のテスト設計技法 → MBTでどう活用されるか

> 同値分割（EP）
> 従来: テスターが手動でクラスを定義し、テストデータを選ぶ
> MBT: モデルのガード条件として定義 → ツールが自動的に各クラスからデータを選択

> 境界値分析（BVA）
> 従来: 境界値付近を手動で特定
> MBT: モデルのデータ仕様に境界値を定義 → 自動的にテストデータに反映

> デシジョンテーブル（DT）
> 従来: 条件と結果の組み合わせ表を手動作成・管理
> MBT: デシジョンテーブル自体がMBTモデルとなり、テストケースを自動生成

> 状態遷移テスト（STT）
> 従来: 状態遷移表から手動でテストケースを設計
> MBT: FSM/状態遷移図がモデルとなり、カバレッジ基準に従って自動生成

> ユースケーステスト（UCT）
> 従来: ユースケース記述からシナリオを手動抽出
> MBT: UMLユースケース/シーケンス図からシナリオを自動生成

### 12.2 MBTとの組み合わせ効果

> 最高の効果を得るための組み合わせ戦略:

> ┌──────────────────────────────────────────────────────────────┐
> │  複雑な業務ロジック（金融・保険・EC等）                        │
> │  → デシジョンテーブル + データベース選択基準                   │
> │  → ペアワイズ/Nwise でデータ組み合わせを最適化                 │
> ├──────────────────────────────────────────────────────────────┤
> │  組み込み・リアクティブシステム（車載・産業機器等）              │
> │  → FSM/状態チャート + 遷移/状態カバレッジ基準                  │
> │  → ミューテーションテストでモデルの品質を検証                   │
> ├──────────────────────────────────────────────────────────────┤
> │  Web/モバイルアプリ                                           │
> │  → UMLアクティビティ図 + シナリオベース選択基準                 │
> │  → Selenium/Appiumアダプターと組み合わせ                       │
> └──────────────────────────────────────────────────────────────┘

---

## 13. MBTツール詳細比較

### 13.1 主要MBTツール一覧

> MBTツールの分類:

> オープンソース
> ├── GraphWalker（最も実用的・普及度高い）
> ├── fMBT（Intel開発、Linux向け）
> ├── MBTsuite（UMLモデル対応）
> └── Modelator（TLA+ベース）

> 商用ツール
> ├── Conformiq Designer（高機能、大企業向け）
> ├── Tricentis TOSCA（ビジネスロジック特化）
> ├── IBM Rational Test Workbench（エンタープライズ向け）
> └── Yakindu Statechart Tools（状態チャート特化）

### 13.2 ツール比較表

> ┌────────────────┬──────────────┬───────────────┬──────────────┬──────────────┐
> │  ツール名       │  ライセンス   │  モデル形式    │  主な用途    │  難易度      │
> ├────────────────┼──────────────┼───────────────┼──────────────┼──────────────┤
> │ GraphWalker    │ OSS（無料）  │ 有向グラフ     │ Web/APIテスト│ ★★☆☆☆    │
> │                │              │ (graphML/JSON)│              │              │
> ├────────────────┼──────────────┼───────────────┼──────────────┼──────────────┤
> │ fMBT           │ OSS（無料）  │ 独自           │ システムテスト│ ★★★☆☆   │
> │                │              │               │ Linux向け    │              │
> ├────────────────┼──────────────┼───────────────┼──────────────┼──────────────┤
> │ MBTsuite       │ OSS          │ UMLダイアグラム│ UMLモデルから│ ★★★★☆   │
> │                │              │               │ 多彩な生成   │              │
> ├────────────────┼──────────────┼───────────────┼──────────────┼──────────────┤
> │ Conformiq      │ 商用          │ 独自+          │ 大規模商用   │ ★★★★☆   │
> │ Designer       │              │ Java/UML      │ プロジェクト │              │
> ├────────────────┼──────────────┼───────────────┼──────────────┼──────────────┤
> │ Tricentis      │ 商用          │ GUI/モデル     │ ビジネスロジック│ ★★★☆☆  │
> │ TOSCA          │              │               │ 回帰テスト   │              │
> ├────────────────┼──────────────┼───────────────┼──────────────┼──────────────┤
> │ IBM RTW        │ 商用          │ UML           │ エンタープライズ│ ★★★★★ │
> │                │              │               │ 統合テスト   │              │
> └────────────────┴──────────────┴───────────────┴──────────────┴──────────────┘

### 13.3 ツール選定フローチャート

> START
> ↓
> Q1: 予算はあるか？
> ├── NO → GraphWalker or fMBT（OSS）
> └── YES → Q2へ

> Q2: 必要なモデル形式は？
> ├── 有向グラフ/FSM → GraphWalker
> ├── UML図 → MBTsuite or IBM RTW
> ├── 状態チャート → Yakindu or MATLAB
> └── ビジネスロジック → TOSCA or Conformiq

> Q3: 既存ツールとの統合は？
> ├── Selenium/Appium必要 → GraphWalker（Java統合が容易）
> ├── Jenkins/CI/CD重視 → GraphWalker or fMBT
> └── SAP/ERP統合 → TOSCA or IBM RTW

---

## 14. GraphWalker実践チュートリアル

### 14.1 GraphWalkerとは

**GraphWalker**はオープンソースのMBTツールで、有向グラフ（Directed Graph）形式のモデルを読み込み、テストパスを自動生成します。

> 特徴:
> ├── ✅ 無料・オープンソース（Apache License）
> ├── ✅ Java製・Maven統合が容易
> ├── ✅ オンライン/オフライン両モード対応
> ├── ✅ WebSocket APIによる外部ツール統合
> ├── ✅ 豊富なパス生成アルゴリズム
> └── ✅ Studio（GUI編集ツール）付属

> 公式: https://graphwalker.github.io/
> GitHub: https://github.com/GraphWalker/graphwalker-project

### 14.2 GraphWalkerモデルの構成要素

> GraphWalkerモデル = 有向グラフ

> ┌────────────────────────────────────────────────────────────────┐
> │  頂点（Vertex）= 状態                                           │
> │  └─ ここで「アサーション（検証）」を行う                         │
> │  └─ 命名規則: v_プレフィックス（例: v_ホーム画面）              │
> │                                                                │
> │  辺（Edge）= 遷移・アクション                                   │
> │  └─ ここで「操作（SUTへの入力）」を行う                          │
> │  └─ 命名規則: e_プレフィックス（例: e_ログインボタンクリック）   │
> │                                                                │
> │  ガード（Guard）                                               │
> │  └─ 辺の通過条件（例: [isLoggedIn==true]）                      │
> │                                                                │
> │  アクション（Action）                                           │
> │  └─ 辺に付随する変数の更新（例: /isLoggedIn=true;）             │
> └────────────────────────────────────────────────────────────────┘

### 14.3 ステップバイステップ：最初のMBTプロジェクト

#### Step 1: Mavenプロジェクトのセットアップ

```xml
<!-- pom.xml の設定 -->
<dependencies>
    <!-- GraphWalker Core -->
    <dependency>
        <groupId>org.graphwalker</groupId>
        <artifactId>graphwalker-core</artifactId>
        <version>4.3.2</version>
    </dependency>
    <!-- GraphWalker Java（テストランナー） -->
    <dependency>
        <groupId>org.graphwalker</groupId>
        <artifactId>graphwalker-java</artifactId>
        <version>4.3.2</version>
    </dependency>
    <!-- Selenium WebDriver（アダプター用） -->
    <dependency>
        <groupId>org.seleniumhq.selenium</groupId>
        <artifactId>selenium-java</artifactId>
        <version>4.18.1</version>
    </dependency>
    <!-- JUnit5 -->
    <dependency>
        <groupId>org.junit.jupiter</groupId>
        <artifactId>junit-jupiter</artifactId>
        <version>5.10.2</version>
        <scope>test</scope>
    </dependency>
</dependencies>

#### Step 2: モデルファイルの作成（JSON形式）

```json
// src/test/resources/models/LoginModel.json
{
  "name": "ログインモデル",
  "models": [
    {
      "name": "LoginModel",
      "id": "m1",
      "startElementId": "e0",
      "vertices": [
        {
          "id": "v1",
          "name": "v_初期画面",
          "requirements": ["REQ-001"],
          "actions": []
        },
        {
          "id": "v2",
          "name": "v_ログイン成功",
          "requirements": ["REQ-002"]
        },
        {
          "id": "v3",
          "name": "v_エラー表示"
        }
      ],
      "edges": [
        {
          "id": "e0",
          "name": "e_アプリ起動",
          "sourceVertexId": "",
          "targetVertexId": "v1"
        },
        {
          "id": "e1",
          "name": "e_正常ログイン",
          "sourceVertexId": "v1",
          "targetVertexId": "v2",
          "guard": "isValidCredential==true",
          "actions": ["isLoggedIn=true;"]
        },
        {
          "id": "e2",
          "name": "e_ログイン失敗",
          "sourceVertexId": "v1",
          "targetVertexId": "v3",
          "guard": "isValidCredential==false"
        },
        {
          "id": "e3",
          "name": "e_ログアウト",
          "sourceVertexId": "v2",
          "targetVertexId": "v1",
          "actions": ["isLoggedIn=false;"]
        },
        {
          "id": "e4",
          "name": "e_再入力",
          "sourceVertexId": "v3",
          "targetVertexId": "v1"
        }
      ]
    }
  ]
}

#### Step 3: モデルインターフェースの定義

```java
// src/test/java/com/example/LoginModel.java
package com.example;

import org.graphwalker.core.machine.ExecutionContext;
import org.graphwalker.java.annotation.GraphWalker;
import org.graphwalker.java.annotation.Vertex;
import org.graphwalker.java.annotation.Edge;

// GraphWalkerアノテーション: どのモデルを使うか & 停止条件を定義
@GraphWalker(
    value = "random(edge_coverage(100))",  // ランダムに探索し全エッジカバレッジ100%を達成
    start = "e_アプリ起動"
)
public interface LoginModel extends ExecutionContext {

    // ★頂点（Vertex）= 検証メソッド
    @Vertex
    void v_初期画面();

    @Vertex
    void v_ログイン成功();

    @Vertex
    void v_エラー表示();

    // ★辺（Edge）= 操作メソッド
    @Edge
    void e_アプリ起動();

    @Edge
    void e_正常ログイン();

    @Edge
    void e_ログイン失敗();

    @Edge
    void e_ログアウト();

    @Edge
    void e_再入力();
}

#### Step 4: テストアダプターの実装

```java
// src/test/java/com/example/LoginTest.java
package com.example;

import org.graphwalker.java.test.TestBuilder;
import org.junit.jupiter.api.AfterEach;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;

import static org.junit.jupiter.api.Assertions.*;

/**
 * MBTアダプタークラス
 * GraphWalkerが生成したパスに沿って、各メソッドが自動的に呼び出される
 */
public class LoginTest implements LoginModel {

    private WebDriver driver;
    private static final String BASE_URL = "http://localhost:8080";

    // テスト変数（モデルのガード条件と同期）
    private boolean isValidCredential = true;

    @BeforeEach
    public void setUp() {
        driver = new ChromeDriver();
        driver.get(BASE_URL);
    }

    @AfterEach
    public void tearDown() {
        if (driver != null) driver.quit();
    }

    // ===== GraphWalker テスト実行 =====
    @Test
    public void runMBTTest() {
        // GraphWalkerがモデルを読み込み、自動的にテストパスを生成・実行
        new TestBuilder()
            .addContext(new LoginTest())
            .execute();
    }

    // ===== 頂点（Vertex）= アサーション（検証） =====

    @Override
    public void v_初期画面() {
        // ログイン画面が表示されていることを確認
        assertTrue(
            driver.findElement(By.id("loginForm")).isDisplayed(),
            "ログインフォームが表示されていません"
        );
        assertFalse(
            driver.getCurrentUrl().contains("/dashboard"),
            "ダッシュボードに遷移してしまっています"
        );
    }

    @Override
    public void v_ログイン成功() {
        // ログイン成功後の画面を確認
        assertTrue(
            driver.findElement(By.id("welcomeMessage")).isDisplayed(),
            "ウェルカムメッセージが表示されていません"
        );
        assertTrue(
            driver.getCurrentUrl().contains("/dashboard"),
            "ダッシュボードに遷移していません"
        );
    }

    @Override
    public void v_エラー表示() {
        // エラーメッセージが表示されていることを確認
        assertTrue(
            driver.findElement(By.id("errorMessage")).isDisplayed(),
            "エラーメッセージが表示されていません"
        );
        assertEquals(
            "IDまたはパスワードが間違っています",
            driver.findElement(By.id("errorMessage")).getText()
        );
    }

    // ===== 辺（Edge）= アクション（操作） =====

    @Override
    public void e_アプリ起動() {
        driver.get(BASE_URL + "/login");
    }

    @Override
    public void e_正常ログイン() {
        isValidCredential = true;
        driver.findElement(By.id("username")).sendKeys("testuser@example.com");
        driver.findElement(By.id("password")).sendKeys("correctPassword");
        driver.findElement(By.id("loginBtn")).click();
    }

    @Override
    public void e_ログイン失敗() {
        isValidCredential = false;
        driver.findElement(By.id("username")).sendKeys("testuser@example.com");
        driver.findElement(By.id("password")).sendKeys("wrongPassword");
        driver.findElement(By.id("loginBtn")).click();
    }

    @Override
    public void e_ログアウト() {
        driver.findElement(By.id("logoutBtn")).click();
    }

    @Override
    public void e_再入力() {
        driver.findElement(By.id("username")).clear();
        driver.findElement(By.id("password")).clear();
    }
}

#### Step 5: パス生成アルゴリズムの選択

> GraphWalkerの主要なパス生成アルゴリズム:

> 1. ランダムウォーク（random）
>    └─ 構文: random(edge_coverage(100))
>    └─ モデルをランダムに探索し、全エッジを指定率でカバー
>    └─ 用途: 探索的テスト、広範なカバレッジ取得

> 2. 最短パス（a_star）
>    └─ 構文: a_star(reached_vertex(v_ログイン成功))
>    └─ 指定の頂点/辺への最短経路を生成
>    └─ 用途: 特定シナリオの最小テストケース生成

> 3. 深さ優先（depth_first）
>    └─ 深いパスを優先的に探索
>    └─ 用途: ネストしたフローの完全探索

> 4. ランダム+停止条件の組み合わせ
>    └─ random(time_duration(30))     → 30秒間ランダム探索
>    └─ random(never)                  → 停止しない（ソークテスト）
>    └─ random(vertex_coverage(100))  → 全頂点100%カバーで停止

---

## 15. MBTの課題と解決策

### 15.1 主要な課題5つ

> 課題1: モデル作成の高い初期コスト
>   問題: モデル設計に時間・スキルが必要
>   解決策:
>   ├── 段階的モデル開発（最初は Happy Path のみ）
>   ├── 既存の設計文書（仕様書・UML）からモデルを再利用
>   └── AIによるモデル自動生成ツールの活用（最新動向）

> 課題2: 状態爆発（State Explosion）
>   問題: 変数・状態の組み合わせでテストケース数が膨大になる
>   解決策:
>   ├── 直交分割アプローチ（コンポーネント分割）
>   ├── ペアワイズ技法による組み合わせ削減
>   └── リスクベースで重要な状態遷移に絞る

> 課題3: テストオラクルの定義困難
>   問題: 期待結果の定義が複雑・曖昧なケースがある
>   解決策:
>   ├── 事前/事後条件を仕様から明示的に定義
>   ├── A/Bオラクル（旧バージョンとの比較）
>   └── 部分的なオラクル（主要プロパティのみ検証）

> 課題4: アジャイルとの相性（重量感）
>   問題: MBTのモデル管理がアジャイルの迅速さに合わない
>   解決策:
>   ├── 軽量ツール（GraphWalker等）の選択
>   ├── モデルをコードとして管理（Model-as-Code）
>   └── CI/CDへの組み込みで自動化

> 課題5: チームのスキル不足
>   問題: モデリングの経験がないメンバーが多い
>   解決策:
>   ├── ISTQB CT-MBT 認定トレーニングへの投資
>   ├── シンプルなFSMから段階的に学習
>   └── ペアリング：テスター + 開発者での協働

### 15.2 実産業での経験から学ぶ（ケーススタディ）

> 事例1: Bombardier社の列車制御システム（GraphWalker使用）
>   対象: 鉄道用列車制御管理システム（TCMS）
>   結果:
>   ├── 手動テストより長いテストシーケンスを自動生成
>   ├── エッジカバレッジが手動テストより高い
>   └── 要件カバレッジは手動と同等を維持
>   出典: ACM Digital Library (2021)

> 事例2: 産業用Web製品（GraphWalker使用）
>   対象: 産業向けWebアプリケーション
>   結果:
>   ├── 以前の手動テストと比較し保守コスト削減
>   ├── テストの再利用性が大幅向上
>   └── Seleniumとの統合でCI/CD組み込みを実現
>   出典: arXiv:2104.02152 (2021)

> 事例3: 金融システム（MBTスイート使用）
>   対象: 複雑な金融計算ロジック
>   結果:
>   ├── テストスイート開発時間を50%削減（2人月 vs 4人月）
>   ├── テストカバレッジ: 38,460ステップ vs 手動2,658ステップ
>   └── プロジェクトコスト64%削減の可能性
>   出典: Grokipedia - Model-based testing (2026)

---

## 16. AI・LLMとMBTの融合（2025年最新動向）

### 16.1 LLMがMBTにもたらす革新

> 従来のMBTの課題:        LLM/AIによる解決:
> ──────────────────── → ──────────────────────────────────
> モデル作成の手間         仕様書・要件からモデルを自動生成
>                         （GPT-4等に「この仕様書からFSMを生成して」と依頼）

> エッジケースの見落とし   AIが過去の欠陥データから見落としやすいケースを提案

> テストデータの生成       AIが境界値・同値クラスを自動特定してデータ生成

> モデルの更新             AIが要件の変更差分からモデルの更新箇所を特定

### 16.2 AI-MBTの最新研究動向（2025年）

> 1. LLMによる自動モデル推論（Automated Model Inference）
>    ├── 自然言語仕様書 → 自動FSM/UML生成
>    ├── EU AIDOaRt プロジェクト（2021〜2024）: AI統合の継続的テスト
>    └── 参考: Grokipedia (2026), ACM FSE (2025)

> 2. ネットワークプロトコルの自動テスト
>    ├── LLMを使った通信プロトコルのMBT自動化
>    └── 参考: IEEE AIIoT 2025

> 3. GUI モデルの自動生成
>    ├── 設計ドキュメントからGUIモデルを自動抽出
>    └── 参考: ACM ASE 2024

> 4. GraphWalkerのAI強化（継続研究）
>    └── 参考: IEEE ICSTW 2025

### 16.3 AIを活用したMBTの実践ヒント（2025年時点）

> Tip 1: LLMでFSMの初期ドラフト生成
>   プロンプト例:
>   「以下の仕様書に基づいて、ログイン機能のFSM（有限状態機械）を
>   GraphWalker JSON形式で生成してください。
>   仕様: [仕様書の内容]」

> Tip 2: AIでテストデータの充実
>   プロンプト例:
>   「このFSMの各遷移のガード条件に対して、
>   同値分割・境界値分析のテストデータを提案してください」

> Tip 3: AIでモデルのレビュー
>   プロンプト例:
>   「以下のFSMモデルに潜在的な問題点やカバレッジの漏れがあれば
>   指摘してください」

---

## 17. ISTQB CT-MBT 試験対策

### 17.1 試験概要

| 項目 | 内容 |
|-----|------|
| 試験名 | Certified Tester Model-Based Tester（CT-MBT）|
| バージョン | Syllabus v1.1 |
| 問題数 | 40問 |
| 合格点 | 26点（65%以上）|
| 試験時間 | 60分（非英語圏 +25% = 75分）|
| 形式 | 選択式（多肢選択） |
| 受験資格 | **CTFL（Foundation Level）認定保持者**が必須 |
| 試験言語 | 英語（他言語対応地域あり） |
| シラバスDL | https://www.istqb.org/?sdm_process_download=1&download_id=3562 |

### 17.2 シラバス章別ウェイトと重要項目

> Chapter 1: MBTの概要（Introduction）
>   ウェイト: 約15%
>   重要項目:
>   ├── MBTの定義（ISTQB用語集の定義を暗記）
>   ├── MBTの目的・動機
>   ├── MBTの成果物（MBTモデル、テストケース、テストアダプター）
>   └── SDLCへの統合（ウォーターフォール vs アジャイル）

> Chapter 2: MBTモデリング
>   ウェイト: 約25%
>   重要項目:
>   ├── モデルの種類（機能/振る舞い/構造）
>   ├── 各モデリング言語の特徴と適用場面
>   ├── FSM・状態チャートの記法
>   └── モデリングのグッドプラクティス

> Chapter 3: テスト選択基準
>   ウェイト: 約25%
>   重要項目:
>   ├── 6つの選択基準ファミリー（完全暗記必須）
>   ├── 構造ベース基準の種類（状態/遷移/パスカバレッジ）
>   ├── 各基準のメリット・デメリット
>   └── 選択基準の適用（問題文の状況から適切な基準を選ぶ）

> Chapter 4: MBTテスト実装・実行
>   ウェイト: 約20%
>   重要項目:
>   ├── オフラインMBT vs オンラインMBTの違い
>   ├── テストアダプターの役割と種類
>   ├── テストオラクルの定義
>   └── 変更管理とテスト再生成

> Chapter 5: MBTの評価・展開
>   ウェイト: 約15%
>   重要項目:
>   ├── MBT展開前の評価基準
>   ├── KPIと効果測定
>   └── MBT導入のリスクと対策

### 17.3 頻出問題パターンと解き方

> パターン1: 「このシステムに最適なモデル記法は？」
>   解き方: システムの特性（状態重視/データ重視/フロー重視）から判断
>   例: 「並行処理・階層構造がある組み込みシステム」→ 状態チャート

> パターン2: 「このカバレッジ基準で何テストケースが必要か？」
>   解き方: 対象の状態数・遷移数・パス数を数えてから計算
>   例: 「遷移カバレッジ100%：FSMの遷移が6本なら最低6ケース必要」

> パターン3: 「オフラインMBTとオンラインMBTの違いは？」
>   解き方: 生成タイミング（事前 vs リアルタイム）で区別
>   例: 「SUTの実際の動作に適応する」→ オンラインMBT

> パターン4: 「このシナリオに適した選択基準は？」
>   解き方: 6ファミリーを覚えて状況から選択
>   例: 「ユーザーストーリーをもとに選択」→ シナリオ/ユースケースベース

> パターン5: 「MBTアクティビティとFundamental Test Processの対応は？」
>   解き方: MBTの各ステップ（モデリング/生成/実装/実行/評価）と
>           CTFL標準のテストプロセスを対応付けて覚える

### 17.4 学習ロードマップ（6週間計画）

> Week 1〜2: 基礎固め
>   ├── CTFL（ISTQB Foundation Level）の復習
>   │   └─ テストプロセス、テスト技法、テストレベルを再確認
>   ├── CT-MBT Syllabus v1.1 を精読（ISTQB公式PDFをDL）
>   └── 用語集（Glossary）の重要用語を暗記

> Week 3: モデリングの実践
>   ├── FSMとデシジョンテーブルを実際に書いてみる
>   ├── UMLステートマシン図の記法を習得
>   └── GraphWalker をインストールして動かしてみる（ハンズオン）

> Week 4: テスト選択基準の深掘り
>   ├── 6ファミリーの各基準をノートに整理
>   ├── 状態カバレッジ / 遷移カバレッジ / パスカバレッジを計算練習
>   └── デシジョンテーブルからのテストケース導出を練習

> Week 5: 模擬試験と弱点補強
>   ├── ISTQB公式 Sample Exam A（Questions + Answers）を解く
>   │   DL: https://istqb.org/?sdm_process_download=1&download_id=3564
>   ├── 間違えた問題の該当シラバス箇所を再読
>   └── ASTQB Sample Exam も挑戦
>   │   URL: https://astqb.org/assets/documents/MBT-Exam-Sample-with-Model-Diagrams.pdf

> Week 6: 最終確認
>   ├── シラバスの章ごとのまとめノートを作成
>   ├── 主要用語の定義を音読で暗記
>   └── 全体を通した最終確認

### 17.5 試験当日のポイント

> ⚠️ 注意事項:
> ├── クローズドブック（持ち込み不可）
> ├── MBTモデルの図が問題に含まれる（図の読み取り力が必要）
> ├── 1問平均1.5分（60分÷40問）
> └── 複数選択より1択が多い（確実な知識が必要）

> ✅ 戦略:
> ├── 確信がある問題から解答
> ├── モデル図の問題は落ち着いて状態・遷移を数える
> ├── 「最も適切な」選択肢を選ぶ（完璧でなくても最善を選択）
> └── 残り10分で未回答・自信なしの問題を見直す

---

## 18. 参考URL一覧

### 📘 ISTQB 公式資料

| 資料 | URL |
|-----|-----|
| CT-MBT 認定ページ（公式） | https://istqb.org/certifications/certified-tester-model-based-tester-ct-mbt/ |
| CT-MBT Syllabus v1.1（DL） | https://www.istqb.org/?sdm_process_download=1&download_id=3562 |
| CT-MBT Sample Exam A Questions（DL） | https://istqb.org/?sdm_process_download=1&download_id=3564 |
| CT-MBT Sample Exam A Answers（DL） | https://istqb.org/?sdm_process_download=1&download_id=3565 |
| ASTQB MBT Sample Exam（英語） | https://astqb.org/assets/documents/MBT-Exam-Sample-with-Model-Diagrams.pdf |
| ISTQB 用語集（Glossary） | https://glossary.istqb.org/en_US/search?term= |

### 🛠️ ツール公式サイト

| ツール | URL |
|-------|-----|
| GraphWalker（公式） | https://graphwalker.github.io/ |
| GraphWalker（GitHub） | https://github.com/GraphWalker/graphwalker-project |
| fMBT（GitHub） | https://github.com/intel/fMBT |
| MBTsuite | https://www.mbtoolbox.org/ |
| Conformiq Designer | https://www.conformiq.com/ |
| Tricentis TOSCA | https://www.tricentis.com/products/automate-continuous-testing-tosca |
| Yakindu Statechart Tools | https://www.itemis.com/en/products/itemis-create/ |
| MATLAB/Simulink Stateflow | https://www.mathworks.com/products/stateflow.html |

### 📚 学習リソース・技術記事

| 資料 | URL |
|-----|-----|
| Model-Based Testing 完全概説（Grokipedia/2026） | https://grokipedia.com/page/Model-based_testing |
| MBT概説（Testsigma/2025） | https://testsigma.com/blog/model-based-testing/ |
| MBTガイド（LambdaTest） | https://www.lambdatest.com/learning-hub/model-based-testing |
| MBTモダンガイド（PractiTest/2025） | https://www.practitest.com/resource-center/blog/model-based-testing-guide/ |
| MBTツール比較（BrowserStack/2025） | https://www.browserstack.com/guide/model-based-testing-tool |
| MBTツールTop10（Testsigma） | https://testsigma.com/blog/model-based-testing-tools/ |
| MBTとテスト自動化（TestGrid/2025） | https://testgrid.io/blog/model-based-testing/ |
| MBT NashTech Blog（2023） | https://blog.nashtechglobal.com/model-based-testing-an-approach-generating-test-cases-from-models-automatically/ |

### 📖 書籍・論文（学術）

| 資料 | URL |
|-----|-----|
| "Model-Based Testing Essentials" O'Reilly（ISTQB公式参考書）| https://www.oreilly.com/library/view/model-based-testing-essentials/9781119130017/ |
| ScienceDirect: MBT Overview | https://www.sciencedirect.com/topics/computer-science/model-based-testing |
| GraphWalker産業事例論文（ACM/2021） | https://dl.acm.org/doi/10.1145/3452383.3452388 |
| GraphWalker産業事例フルテキスト（arXiv/2021） | https://arxiv.org/pdf/2104.02152 |
| IEEE: State Transition Testing カバレッジ基準 | https://ieeexplore.ieee.org/document/7052254/ |
| ResearchGate: カバレッジ基準体系的レビュー | https://www.researchgate.net/publication/330047546 |

### 🎓 トレーニング・認定機関

| 機関 | URL |
|-----|-----|
| ISTQB 試験プロバイダー検索 | https://istqb.org/exam-providers/?directory_type=exam-providers |
| ISTQB トレーニングプロバイダー検索 | https://istqb.org/training-providers/?directory_type=training-providers |
| Processworks CT-MBT コース | https://www.processworksgroup.com/course/ctfl-mbt/ |
| ISTQB SCR（資格確認） | http://scr.istqb.org/ |

---

## 🎯 まとめ：MBT習得のための10原則

> 1. 🧠 「モデル」は「抽象化」である
>    └─ すべての詳細を入れるのではなく、テスト目的に必要な部分のみを表現

> 2. 🔺 シンプルなFSMから始める
>    └─ 最初からUMLや状態チャートは不要。まずFSMで感覚をつかむ

> 3. 🎯 選択基準を意識してモデルを設計する
>    └─ 「何をカバーしたいか（遷移/状態/パス等）」を先に決める

> 4. 🔄 モデルは「生きた文書」として継続更新
>    └─ 一度作ったら終わりではなく、要件変更に合わせて常に更新

> 5. 📍 トレーサビリティを必ず維持する
>    └─ 要件↔モデル↔テストケースの対応を明示的に管理

> 6. 🛠️ まずGraphWalkerを動かしてみる
>    └─ 無料・設定簡単・Javaエコシステムと親和性高い

> 7. ⚡ オフライン/オンラインMBTを使い分ける
>    └─ CI/CDにはオフライン、探索的テストにはオンラインが向いている

> 8. 🤝 MBTは従来技法の「置き換え」ではなく「拡張」
>    └─ 同値分割・境界値分析・デシジョンテーブルの知識が活きる

> 9. 🏭 小規模Pilotから始めて効果を測定する
>    └─ いきなり全社展開せず、1機能・1チームで試してROIを確認

> 10. 🤖 AI/LLMとの連携で生産性を高める
>     └─ 仕様書からのモデル生成補助・テストデータ提案にLLMを活用

---

> 📌 **著者注記:** このガイドは ISTQB CT-MBT Syllabus v1.1、GraphWalker公式ドキュメント、ACM/IEEE 学術論文、および2025〜2026年の最新業界情報を基に作成されています。  
> 試験情報は変更される可能性があるため、受験前に必ず公式サイト（https://istqb.org）で最新情報を確認してください。
